module.exports = {
  name: 'video',
  description: '(placeholder) generic video downloader',
  category: 'download',
  execute: async (message, botInstance) => {
    return { success: true, message: '📹 video command placeholder — provide a URL.' };
  }
};
