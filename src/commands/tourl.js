module.exports = {
  name: 'tourl',
  description: 'Upload media to URL',
  category: 'tools',
  execute: async () => {
    return { success: true, message: `*_🔗 Reply to media with .tourl to get a shareable link_*` };
  }
};
