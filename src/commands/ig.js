module.exports = {
  name: 'ig',
  description: '(placeholder) Instagram download',
  category: 'download',
  execute: async (message, botInstance) => {
    return { success: true, message: '📷 ig command placeholder — send an Instagram URL.' };
  }
};
