module.exports = {
  name: 'aiimg',
  description: '(placeholder) Generate image via AI',
  category: 'ai',
  execute: async (message, botInstance) => {
    return { success: true, message: '🖼️ aiimg placeholder — integrate with an image API (needs API key).' };
  }
};
